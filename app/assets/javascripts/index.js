$(function() {
  // var slick = $('.slider').slick({
  //   slick.slick('reinit');
  // });

  $('.slider').slick({
    arrows: true,
    dots: true,
    autoplay: true,
    autoplaySpeed: 2000,
    speed: 600
  });
});