.members
  .member-index
    メンバー 一覧 
    %br
    （3人まで登録できます）
  .member-selects
    .select
      - @members.each_with_index do |member, i|
        - if current_user.already_favorited?(member)
          .add-member
            = member.name
            .registration
            = link_to "推しメンから外す" , favorite_path(@favorites.ids), method: :delete, class: "delete"
        - else
          .add-member
            = member.name
            -if @count.count == 3

            -else
              = link_to "推しメンに登録する", add_favorite_path(id: member.id), method: :post