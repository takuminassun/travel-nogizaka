.contents
  - @comments.each do |comment|
    .content
      .content__left
        .content__left--place
          = comment.place
      .content__right
        .content__right-name
          - comment.member.each do |member|
            = member.name
      =link_to '詳細', "comments/#{comment.id}", method: :get
            